<h1>Add a Task List</h1>


<%= form_for @task_list, method: :post do |f| %>
  <% if @task_list.errors[:name].any? %>
    <p class="error"><%= @task_list.errors[:name].first %></p>
  <% end %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit "Create Task List" %>
<% end %>